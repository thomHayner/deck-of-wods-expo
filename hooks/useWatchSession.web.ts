/**
 * Web stub — Metro uses this file instead of useWatchSession.ts on web.
 * All watch connectivity is iOS/Android-only; no-ops keep the web bundle clean.
 */

export function useWatchSession() {}
export function useWatchMessageListener() {}
export function useSyncHistoryToWatch() {}
